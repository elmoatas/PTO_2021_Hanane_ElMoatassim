using Commuting;
using System.Windows;

namespace WpfApp4
{
    /// <summary>
    /// Interaction logic for CarWindow.xaml
    /// </summary>
    public partial class TrainWindow : Window
    {
        public TrainWindow()
        {
            InitializeComponent();
        }

        private void addButton_Click(object sender, RoutedEventArgs e)
        {
           // Commute bike = new Bike(fromTextBox.Text, toTextBox.Text, datePicker.SelectedDate.ToString(), 0);
        }
    }
}
